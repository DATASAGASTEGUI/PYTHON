#include <stdio.h>
#include <math.h>

int main() {
    int base = 2;
    int exponente = 3;
    float potencia = pow(base,exponente);
    printf("Potencia dos al cubo: %.2f", potencia);

    return 0;
}